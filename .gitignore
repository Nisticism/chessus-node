node_modules
logo-options/
.env
.env.production
.env.local
.env.development.local
.env.test.local
.env.production.local
authServer-old.js
seed-old.js
profile-pictures
uploads/

# JWT secrets file (auto-generated, do not commit)
server/.jwt-secrets.json

# Git push token used by scripts/git-push.sh (never commit; template is
# .git-credentials.env.example)
.git-credentials.env
sound-library-backup/
node_modules

# AI training artifacts (models, logs, rules dumps)
ai-training/

# Rust build output
ai-engine-rs/target/
ai-engine-rs/Cargo.lock

# Compiled trainer binaries — tracked via Git LFS (not regular git)
# Ignore everything inside trainer-binaries EXCEPT the actual binaries.
trainer-binaries/**
!trainer-binaries/
!trainer-binaries/win32/
!trainer-binaries/win32/ai-engine.exe
!trainer-binaries/linux/
!trainer-binaries/linux/ai-engine

# Local database dumps (production data copies — never commit)
chessusnode_prod.sql
*_prod.sql

# Dev-only production DB tunnel + pull scripts (contain/read RDS secrets)
scripts/dev-db/

# Local MCP config
.vscode/mcp.json
