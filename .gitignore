node_modules
logo-options/
.env
.env.production
.env.local
.env.development.local
.env.test.local
.env.production.local
authServer-old.js
seed-old.js
profile-pictures
uploads/

# JWT secrets file (auto-generated, do not commit)
server/.jwt-secrets.json
sound-library-backup/
node_modules

# AI training artifacts (models, logs, rules dumps)
ai-training/

# Rust build output
ai-engine-rs/target/
ai-engine-rs/Cargo.lock

# Compiled trainer binaries — tracked via Git LFS (not regular git)
trainer-binaries/
# Allow the actual binaries to be tracked (LFS handles storage)
!trainer-binaries/win32/ai-engine.exe
!trainer-binaries/linux/ai-engine
